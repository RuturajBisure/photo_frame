.main-container{:style => "font-size: 20px;color: #333;"}
  %ul#lightgallery.list-unstyled.row
    - @images.each do |image| 
      %li.col-md-2{:style => "margin-bottom:10px;"}
        - imag = image.photo.url(:large)
        %a.fancybox-thumb{:href => "#{imag}", :rel => "fancybox-thumb"}
          = image_tag(image.photo.url(:small), :height => "max-height")
        %p{:style => "color:#000;"}
          %strong
          Tagline of the image:- #{image.tagline}
        %p{:style => "color:#000;"}
          %strong
          OWNER NAME:- #{image.owner}
        %p{:style => "color:#000;"}
          %strong
          Created At:- #{image.created_at}        
        = link_to 'destroy', image_path(image), method: :delete, data: { clear:'Are you sure?'}, :style => "text-decoration:none"
  = link_to "New image", new_image_path, :class => "btn btn-success", :style => "text-decoration:none"

:javascript
  $(document).ready(function() {
    $(".fancybox-thumb").fancybox({
      prevEffect  : 'none',
      nextEffect  : 'none',
      helpers : {
        title : {
          type: 'outside'
        },
        thumbs  : {
          width : 50,
          height  : 50,
        }
      }
    });
  });
 